# Hotfix 0.6.1

This hotfix replaces the shared UI component files required by Mission Control.

Upload the `components` folder to the repository root and replace the existing files.

Required final paths:

- components/__init__.py
- components/cards.py
- components/theme.py
